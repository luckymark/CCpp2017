#include<stdio.h>
#include<time.h>
int main()
{
    int l[1000];
    for(int i=0;i<999;i++)
    {
        l[i]=i+2;
    }
    for(int i=0;i<999;i++)
    {
        if(l[i]!=0)
        {
            printf("%d\n",l[i]);
            for(int j=2;j<=1000/l[i];j++)
            {
                l[l[i]*j-2]=0;
            }
        }
    }
    printf("time: %lu\n",clock());
    return 0;
}
