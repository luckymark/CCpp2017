#include<stdio.h>
int main()
{
    int n,a=1,b;
    scanf("%d",&n);
    int l[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&l[i]);
    }
    while(a!=0)
    {
        a=0;
        for(int i=0;i<n-1;i++)
        {
            if(l[i]>l[i+1])
            {
                b=l[i];
                l[i]=l[i+1];
                l[i+1]=b;
                a++;
            }
        }
    }
    for(int i=0;i<n;i++)
    {
        printf("%d ",l[i]);
    }
    printf("\n");
    return 0;
}
